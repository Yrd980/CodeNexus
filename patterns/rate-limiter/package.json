{
  "name": "@codenexus/rate-limiter",
  "version": "1.0.0",
  "description": "Production-ready rate limiting with multiple algorithms and pluggable storage",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
